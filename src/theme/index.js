import { fonts, fontSizes, fontWeights } from "./fonts";
import { colors } from "./colors";
import { globals } from "./globals";
import { devices, breakpoints } from "./breakpoints"

export const theme = {
  breakpoints,
  devices,
  fonts,
  fontSizes,
  fontWeights,
  colors,
  globals
}